resources:
  jobs:
    bootstrap_env_setup:
      name: bootstrap-env-setup
      tasks:
        - task_key: bootstrap-env-setup
          notebook_task:
            notebook_path: /Repos/aamir.mscse@gmail.com/Lending-Risk-Framework/src/notebooks/00_setup/00_bootstrap_env
            source: WORKSPACE
          environment_key: Default
      queue:
        enabled: true
      parameters:
        - name: catalog_name
          default: lending_risk_dev
      run_as:
        user_name: aamir.mscse@gmail.com
      environments:
        - environment_key: Default
          spec:
            environment_version: "5"
      performance_target: PERFORMANCE_OPTIMIZED
