# This is a Databricks asset bundle definition for my_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Lending-Risk-Framework
  uuid: 4eece1e1-97de-4c3d-96e6-f685a248387c

include:
  - resources/*.yml
  - resources/*/*.yml


targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405607628514849.9.azuredatabricks.net
    
  prod:
    mode: production
    workspace:
      host: https://adb-7405607628514849.9.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/aamir.mscse@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/aamir.mscse@gmail.com/.bundle/${bundle.name}/${bundle.target}

    permissions:
      - user_name: aamir.mscse@gmail.com
        level: CAN_MANAGE
